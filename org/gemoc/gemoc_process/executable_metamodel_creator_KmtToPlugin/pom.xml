<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <name>executable_metamodel_creator_KmtToPlugin</name>
  <url>http://maven.apache.org</url>

  <parent>
  	<artifactId>org.gemoc.gemoc_process.root</artifactId>
    <groupId>org.gemoc.gemoc_process</groupId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>

  <artifactId>executable_metamodel_creator_KmtToPlugin</artifactId>
  <packaging>bundle</packaging>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
  	<dependency>
        <groupId>org.gemoc.gemoc_process</groupId>
	    <artifactId>org.gemoc.gemoc_process.executable_metamodel_creator</artifactId>
        <version>0.0.1-SNAPSHOT</version>
  	</dependency>
  </dependencies>
  
  <build>
  	<plugins>
  		<plugin>
  			<groupId>org.apache.felix</groupId>
            <artifactId>maven-bundle-plugin</artifactId>
            <version>2.3.5</version>
            <extensions>true</extensions>
            <configuration>
	            <instructions>
	            	<Bundle-SymbolicName>${project.artifactId};singleton:=true</Bundle-SymbolicName>
			        <Bundle-Name>${project.name}</Bundle-Name>
			        <Bundle-Vendor>${bundle.vendor}</Bundle-Vendor>
					<Bundle-ActivationPolicy>lazy</Bundle-ActivationPolicy>
			        <Bundle-ClassPath>.</Bundle-ClassPath>
					<Bundle-Version>${bundle.version}</Bundle-Version>
					<Bundle-Activator>org.gemoc.gemoc_process.executable_metamodel_creator.Activator</Bundle-Activator>
	                <!-- Dependency used for compiling all the bundle (with dependency to org.eclipse.core.* -->
	                <Require-Bundle>
			        </Require-Bundle>
			        <!-- note: imports k2 to workaround problem about loading reflexivity.km
			        and reexport it to allow an external plugin (ui for ex) to set the messagingsystem of stdio -->
	                <Export-Package>org.gemoc.gemoc_process.executable_metamodel_creator
			        </Export-Package>
			        <Private-Package>
			        	gemoc_process_executable_metamodel_creator.*,
			        	gemoc_process_executable_metamodel_creatorrunner.*,
			        	ScalaImplicit.gemoc_process_executable_metamodel_creator.*,
			        	scalaUtil;-split-package:=merge-last,
				    	org.gemoc.gemoc_process.executable_metamodel_creator.impl,
				    	kermeta_standard.*
			       	</Private-Package>
			       <!--
			       		specify the packages that should be imported to correctly build
			        	the bundle (i.e <packaging>bundle<packaging> ".*" to also include
			        	sub packages use the ! for avoiding to import some packages that
			        	are already there with the Require-Bundle
				    -->
			        <Import-Package>
			           	*
			        </Import-Package>		            
	             </instructions>
             </configuration>
  		</plugin>
  	</plugins>
  </build>
  
</project>
