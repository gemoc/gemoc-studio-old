using kermeta::standard::* 
using kermeta::io::StdIO => stdio
using tfsm::*

package tfsm{
	aspect class State{  
	 operation onEnter() : String is do
	    result:="onEnter: " + self.name
	 end
	 
	 operation onLeave() : String is do
	 	result:="onLeave: " + self.name
	 end
	}
}